= form_for @supplier_type do |f|
  - if @supplier_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@supplier_type.errors.count, "error")} prohibited this supplier_type from being saved:"
      %ul
        - @supplier_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :details
    = f.text_field :details
  .actions
    = f.submit 'Save'
